export interface SelfClassCodeResult {
  id: string;
  code: string;
  topic_id: string;
  topic_name?: string;
  valid_from: string;
  valid_until: string;
  message_id: string;
  conversation_id: string;
}
